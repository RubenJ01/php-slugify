when:
  - event: [push, pull_request, manual]

steps:
  - name: test
    image: php:8.4-cli
    environment:
      DEBIAN_FRONTEND: noninteractive
    commands:
      - apt-get update -qq && apt-get install -y -qq git unzip libicu-dev > /dev/null 2>&1
      - docker-php-ext-install intl > /dev/null 2>&1
      - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
      - composer update --no-interaction --prefer-dist --quiet
      - echo "══════ PHP CodeSniffer (PSR-12) ══════"
      - vendor/bin/phpcs --standard=PSR12 src/
      - echo "══════ PHPStan (Level 9) ══════"
      - vendor/bin/phpstan analyse --configuration=phpstan.neon --no-progress
      - echo "══════ PHPUnit ══════"
      - vendor/bin/phpunit --colors=always
